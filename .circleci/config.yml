version: 2
jobs:
  build:
    docker:
      - image: circleci/node:6

    steps:
      - checkout
      - run:
          name: install yarn
          command: npm run install-local-yarn
      - run:
          name: install dependencies
          command: npm run install-deps-with-local-yarn
